---
category: Operators
---

# if
Switches between two options based on another value.

#### Syntax
```
boolean ? value : value
if(boolean, value, value)
```

#### Examples
```
true ? 1 : -1 == 1
if(false, "yes", "no") == "no"
```
